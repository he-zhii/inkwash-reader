{
  "name": "pages-beautify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "rebuild": "node scripts/rebuild-index.js",
    "start": "npx -y http-server dist -p 3000 -c-1",
    "dev": "node scripts/rebuild-index.js && npx -y http-server dist -p 3000 -c-1"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
